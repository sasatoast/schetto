# ===========================================
# Supabase データベース接続
# ===========================================
# 1. Supabase ダッシュボードでプロジェクトを作成
# 2. [Connect] → [Session Pooler] の接続文字列をコピー
# 3. パスワードをプロジェクト作成時に保存したものに置き換え
# 4. このファイルを .env にコピーし、値を設定
#    cp .env.example .env
#
# Supabase は SSL 必須のため、接続文字列に ?sslmode=require を付与するか、
# DATABASE_SSL_MODE=require を設定してください。
# ===========================================

# 開発環境用（Supabase Session Pooler の接続文字列）
# 例: postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?sslmode=require
DATABASE_URL=

# Supabase 接続時に SSL を必須にする場合（DATABASE_URL に sslmode を付けない場合）
# DATABASE_SSL_MODE=require
